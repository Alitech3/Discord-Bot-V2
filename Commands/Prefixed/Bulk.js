module.exports = {
	name: '%bulk',
	aliases: undefined,
	description: 'no.',
	ExpandedDesc: 'no again.',
	// eslint-disable-next-line no-unused-vars
	execute(prefCommand, message, ytdl, bot, Discord) {
		// console.log(message.channel.bulkDelete(10));
	}
};